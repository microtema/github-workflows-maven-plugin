%STAGE_DISPLAY_NAME%:
  <<: *stage-template
  stage: regression
  script:
    - mvn integration-test -P %PROFILE_NAME% -DstageName=%STAGE_NAME% $MAVEN_CLI_OPTS
  artifacts:
    paths:
      - target
    expire_in: 6 hour
  only:
    refs: %REFS%
