Security Check:
  <<: *stage-template
  stage: security
  script:
    - mvn dependency-check:help -P security -Ddownloader.quick.query.timestamp=false $MAVEN_CLI_OPTS
  only:
    refs:
      - /^feature/.*$/
      - develop
      - /^release/.*$/
